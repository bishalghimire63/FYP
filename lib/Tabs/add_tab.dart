import 'package:flutter/material.dart';

class AddItemTab extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Add Item tab"),
      ),
    );
  }
}
